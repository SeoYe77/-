# 基于城市场景仿真序列的低空多目标跟踪滤波方法研究

## 摘要
面向城市场景下6—8架低空无人机目标在尺度变化、检测噪声、短时漏检和遮挡条件下容易发生轨迹中断与ID切换的问题，本文提出一种基于城市场景仿真序列的低空多目标跟踪滤波方法。该方法由检测序列预处理、卡尔曼滤波状态预测和数据关联更新三部分组成：首先对检测结果进行置信度筛选与尺度归一化，以降低误检和检测抖动对关联过程的影响；随后利用卡尔曼滤波对目标位置和尺度进行时序预测，以补偿短时漏检并缓解透视投影引起的尺度波动；最后结合匈牙利算法、IOU和马氏距离完成目标匹配，并通过轨迹确认与保留机制降低误匹配与ID切换。实验结果表明，该方法能够有效提升城市场景仿真序列中的多目标跟踪连续性，减少ID切换，提高低空多目标跟踪滤波的稳定性和鲁棒性。

**关键词：** 城市场景仿真；低空无人机；多目标跟踪；卡尔曼滤波；ID切换

## 1 引言
随着低空领域开放与无人机技术的快速发展，低空无人机目标在城市空域中的监测、告警与处置需求日益增长。由于城市建筑遮挡、目标尺度随距离快速变化、检测噪声和短时漏检等因素的共同影响，低空多目标跟踪过程中容易出现轨迹碎片化、跟踪中断和ID切换频繁等问题，难以满足稳定、连续的跟踪需求。

目标检测与多目标跟踪是低空目标感知的关键环节，其中检测结果的稳定性和数据关联的准确性直接决定后续跟踪性能。近年来，YOLO系列检测器因其实时性和检测精度被广泛用于无人机目标检测；DeepSORT、ByteTrack、BoT-SORT等多目标跟踪方法通过运动预测、数据关联和轨迹管理提升了复杂场景下的跟踪鲁棒性。尤其是卡尔曼滤波在状态估计与短时预测方面具有良好效果，能够有效缓解遮挡、漏检和观测噪声带来的影响。

然而，真实低空无人机视频数据获取困难、场景可控性差、标注成本高，难以支持可重复、可对比的算法验证。因此，本文基于城市场景仿真序列构建低空多目标跟踪实验条件，在6—8架无人机的仿真检测序列上，对不同关联方式与运动预测方法进行分析，重点研究卡尔曼滤波在降低ID切换、提升轨迹连续性方面的作用，并进一步提出一种适用于城市场景仿真序列的低空多目标跟踪滤波方法。

本文的主要工作与贡献如下：
1. 基于城市场景仿真序列构建低空多目标跟踪任务，统一考虑透视投影、检测噪声、漏检和误检等因素；
2. 提出融合检测预处理、卡尔曼滤波预测和多度量数据关联的低空多目标跟踪滤波方法；
3. 针对ID切换问题，引入轨迹确认与保留机制，增强复杂城市场景下的轨迹稳定性；
4. 通过仿真实验验证所提方法在减少ID切换和提升跟踪连续性方面的有效性。

## 2 系统总体描述与问题定义
本文研究的对象为城市场景下的低空多目标跟踪任务，重点分析噪声、漏检、尺度变化和遮挡条件下的关联稳定性问题。

设场景内共有N架低空无人机目标，在时刻t，第k个目标的真实状态表示为：

$$X_k(t) = [x(t), y(t), z(t), \dot{x}(t), \dot{y}(t), \dot{z}(t)]^T$$

其中，$(x,y,z)$表示三维空间位置，$(\dot{x},\dot{y},\dot{z})$表示速度。

通过虚拟相机的透视投影，三维目标投影到图像平面后得到二维检测框：

$$b_k(t) = [u(t), v(t), w(t), h(t)]^T$$

其中，$(u,v)$为检测框中心坐标，$w,h$为宽和高。

在实际观测中，检测结果受到噪声、误检和漏检影响，可表示为：

$$z_k(t) = b_k(t) + n_k(t)$$

其中，$z_k(t)$为检测器输出，$n_k(t) \sim N(0,R)$为高斯观测噪声。

多目标跟踪的目标是从含噪、不完整的检测序列中恢复连续轨迹，并为每个目标分配稳定ID。为此，本文采用以检测结果为输入、以运动预测和数据关联为核心的低空多目标跟踪滤波方法，对城市场景仿真序列中的轨迹连续性和ID切换问题进行分析与优化。

## 3 城市场景多无人机仿真与检测序列生成
### 3.1 三维场景与目标运动模型
仿真场景定义为城市低空三维空间：

$$\Omega = [X_{\min}, X_{\max}] \times [Y_{\min}, Y_{\max}] \times [Z_{\min}, Z_{\max}]$$

本文默认设置场景内无人机目标数量为7架，以覆盖城市低空小规模多目标跟踪的典型情形。

城市建筑群采用随机生成方式构建，以模拟真实楼宇遮挡环境。禁飞区采用球体模型表示，并通过吸引行为与边界反弹机制控制目标运动，使目标在仿真空间内形成更接近真实低空环境的运动轨迹。

### 3.2 虚拟相机模型与透视投影
虚拟相机采用针孔成像模型，三维空间点投影到二维图像平面后，目标尺度会随深度变化而改变。由于目标距离相机越远，检测框越小，因此同一目标在相邻帧之间可能出现显著尺度波动。这一现象会降低IOU匹配的稳定性，是造成ID切换的重要原因之一。

### 3.3 检测噪声与漏检/误检建模
为模拟真实检测器输出，本文对检测结果引入高斯位置噪声、高斯尺度噪声、伯努利漏检和泊松误检模型。由此生成的检测序列包含真实目标检测框与误检框，并输出为标准化格式，能够直接作为多目标跟踪算法输入。

上述仿真设置使检测序列具有较强挑战性，尤其是在尺度快速变化、短时漏检和误检干扰条件下，对后续数据关联与轨迹管理提出了更高要求。

## 4 低空多目标跟踪滤波方法
### 4.1 多目标跟踪问题形式化定义
多目标跟踪（Multi-Object Tracking, MOT）的核心任务是：从逐帧检测框序列中，为每个目标分配唯一且稳定的身份标识（ID），并输出连续的时空轨迹。

设仿真序列总帧数为T，第t帧（t=1,2,...,T）的检测结果为$D_t = \{z_i^t\}_{i=1}^{M_t}$，其中$M_t$为第t帧检测框数量，单个检测框$z_i^t = [x_i^t, y_i^t, w_i^t, h_i^t]^T$表示第i个目标的左上角坐标与宽高（像素）。

多目标跟踪的输出为轨迹集合
$\mathcal{T} = \{\tau_j\}_{j=1}^{N_{track}}$。
其中单条轨迹为：

$$\tau_j = \{(z_{j,t}, t) \mid t \in [t_{j,start}, t_{j,end}]\}$$

$N_{track}$为生成的独立轨迹ID总数。跟踪的核心目标是：在检测噪声、尺度变化、随机漏检等干扰下，实现检测框与轨迹的最优数据关联，最小化轨迹碎片化与ID切换，最大化轨迹连续性与ID保持能力。

### 4.2 基线方法：IOU-greedy 无预测关联算法
#### 4.2.1 算法原理
IOU-greedy是典型的无运动预测跟踪方法，其核心逻辑为仅依赖帧间检测框的交并比（Intersection over Union, IOU）进行贪心匹配，无卡尔曼滤波预测、无轨迹状态管理、无时序先验约束。

对于第t帧的每个检测框$z_i^t$，计算其与第$t-1$帧所有轨迹末端检测框$z_j^{t-1}$的IOU：

$$IOU(z_i^t, z_j^{t-1}) = \frac{Area(z_i^t \cap z_j^{t-1})}{Area(z_i^t \cup z_j^{t-1})}$$

选取IOU最大且超过预设阈值的轨迹进行匹配；若无满足条件的轨迹，则为该检测框新建轨迹ID。该方法逻辑简单、计算高效，但完全依赖局部空间重叠，对尺度变化、噪声扰动和短时漏检较为脆弱。

#### 4.2.2 算法局限性分析
在反无人机集群场景中，IOU-greedy的局限性被显著放大：
1. 变化导致IOU失效：由3.2.3节透视投影关系$w,h \propto 1/Z$可知，目标深度变化会引发检测框尺度剧烈波动，相邻帧同一目标的IOU急剧下降，匹配失败概率大幅提升；
2. 扰动加剧匹配歧义：高斯位置噪声与尺度噪声会导致检测框位置、尺寸抖动，进一步降低帧间IOU，引发误匹配；
3. 直接导致轨迹断裂：无运动预测时，短时漏检会使轨迹失去匹配源，直接终止轨迹，目标重出现时触发新ID生成，最终导致严重轨迹碎片化。

### 4.3 DeepSORT跟踪方法与卡尔曼滤波核心作用
#### 4.3.1 轨迹状态表示与卡尔曼滤波
DeepSORT（Simple Online and Realtime Tracking with a Deep Association Metric）是基于检测的跟踪（Tracking-by-Detection）方法，通过卡尔曼滤波运动预测、匈牙利算法数据关联、轨迹生命周期管理三大核心模块，解决无预测方法的轨迹碎片化问题。本文重点突出卡尔曼滤波的运动预测作用，并结合外观特征作为理论补充，不强依赖其外观分支。

DeepSORT采用8维状态向量描述单条轨迹的运动状态：

$$x = [u, v, \gamma, h, \dot{u}, \dot{v}, \dot{\gamma}, \dot{h}]^T$$

其中$(u,v)$为检测框中心坐标，$\gamma=w/h$为宽高比，$h$为框高，$(\dot{u},\dot{v},\dot{\gamma},\dot{h})$为对应状态的速度分量。

卡尔曼滤波分为预测与更新两个核心步骤：

1. 预测步骤（Predict）：基于上一时刻状态$x_{t-1}$与状态转移矩阵$F$，预测当前时刻的状态$\hat{x}_t$与协方差$\hat{P}_t$：

$$\hat{x}_t = Fx_{t-1}$$

$$\hat{P}_t = FP_{t-1}F^T + Q$$

其中$Q$为过程噪声协方差矩阵，用于描述运动模型的不确定性。预测步骤为数据关联提供时序先验，使匹配不再完全依赖局部IOU。

2. 更新步骤（Update）：融合当前帧检测观测$z_t$，对预测状态进行修正，得到最优后验估计$x_t$与协方差$P_t$：

$$K_t = \hat{P}_tH^T(H\hat{P}_tH^T + R)^{-1}$$

$$x_t = \hat{x}_t + K_t(z_t - H\hat{x}_t)$$

$$P_t = (I - K_tH)\hat{P}_t$$

其中$K_t$为卡尔曼增益，$H$为观测矩阵，$R$为观测噪声协方差矩阵。更新步骤实现观测与预测的融合，平滑轨迹抖动，抑制噪声影响。

#### 4.3.2 数据关联机制
DeepSORT采用运动信息与IOU联合关联方法，构建关联代价矩阵：

$$C_{ij} = \lambda \cdot d_{mahalanobis}(z_i, \tau_j) + (1-\lambda) \cdot (1-IOU(z_i, \tau_j))$$

其中$d_{mahalanobis}$为马氏距离，用于衡量检测框与轨迹预测状态的运动一致性；$\lambda$为权重系数，本文由于仿真数据外观简化，取1，完全依赖运动信息关联。基于代价矩阵，采用匈牙利算法求解最优匹配，实现检测框与轨迹的一一对应；同时通过马氏距离门限过滤异常匹配，降低ID切换概率。

#### 4.3.3 轨迹生命周期管理
DeepSORT通过轨迹状态管理机制，进一步提升跟踪稳定性：
1. 轨迹初始化：新检测框首次匹配时，创建tentative（待定）轨迹；
2. 轨迹确认（confirmed）：若待定轨迹连续多帧成功匹配，则升级为confirmed轨迹，视为有效稳定轨迹；
3. 轨迹保留（max_age）：当轨迹短时漏检时，通过卡尔曼滤波持续预测状态，保留轨迹max_age帧，避免漏检导致的轨迹断裂；
4. 轨迹删除：若轨迹超过max_age帧无匹配，则删除该轨迹。

该机制有效抑制了漏检引发的轨迹重生，大幅提升轨迹连续性。

#### 4.3.4 卡尔曼滤波在跟踪中的核心贡献
卡尔曼滤波是DeepSORT提升关联稳定性的核心模块，其作用可总结为：
1. 时序先验提供匹配依据：预测步骤为数据关联提供目标位置的时序预测，使匹配不再完全依赖局部IOU，解决尺度变化、噪声扰动导致的IOU失效问题；
2. 短时漏检��偿轨迹连续性：在无观测时持续预测目标位置，保留轨迹状态，避免漏检引发的轨迹断裂与ID重生；
3. 噪声平滑抑制轨迹抖动：融合观测与预测，平滑检测噪声导致的框位置、尺寸抖动，提升轨迹输出的稳定性；
4. 门限过滤降低误匹配：通过马氏距离门限过滤异常检测，减少误匹配与ID切换，提升关联准确性。

## 5 仿真结果及分析
### 5.1 实验设置与数据说明
为验证本文构建的城市场景无人机检测序列生成模型的真实性与挑战性，以及跟踪方法在复杂观测条件下的鲁棒性，本文基于400帧标准化仿真检测序列开展定量统计与对比实验。实验所用检测序列由uav_simulator.m生成，包含透视投影畸变、高斯位置噪声、尺度噪声、随机漏检与泊松误检，能够较完整地模拟城市场景低空多目标跟踪的典型挑战。

### 5.2 检测序列统计特性与挑战性分析
本节从时序统计分布、检测置信度、目标尺度变化、观测抖动强度四个维度，对仿真检测序列进行全面量化分析，揭示跟踪难点的来源与机理。

### 5.3 跟踪输出稳定性对比分析
本节基于具有挑战性的检测序列，对IOU-greedy基线与引入卡尔曼滤波的DeepSORT跟踪方法进行量化对比，从活跃轨迹数、累计ID增长和轨迹长度分布等指标验证运动预测的核心作用。

### 5.4 结果成因分析
两种方法的性能差异本质源于运动预测的有无。IOU-greedy仅依赖帧间IOU匹配，无运动预测与时序先验，在透视投影引发的尺度变化、高斯噪声扰动和随机漏检共同作用下，相邻帧同一目标的IOU急剧下降，匹配频繁失败，最终导致轨迹碎片化与ID爆炸。DeepSORT则通过卡尔曼滤波提供的运动预测为数据关联提供时序先验，使匹配不再完全依赖局部IOU，并结合轨迹生命周期管理机制进一步抑制轨迹断裂和ID重生问题。

综上，卡尔曼滤波（运动预测）在反无人机集群仿真场景中，对降低轨迹碎片化、提升多目标跟踪关联稳定性具有不可替代的关键作用。

## 6 结论
本文面向城市场景仿真序列下的低空多目标跟踪任务，围绕尺度变化、检测噪声、短时漏检与ID切换等问题，提出一种融合检测序列预处理、卡尔曼滤波预测和多度量关联的低空多目标跟踪滤波方法。实验表明，该方法能够有效提升轨迹连续性，减少ID切换，并增强复杂城市场景下多目标跟踪的稳定性与鲁棒性。后续工作可进一步引入更丰富的外观特征与更复杂的三维运动模型，以提升在更强遮挡和更大规模目标场景中的适用性。

## 参考文献
1. Wojke N, Bewley A, Paulus D. Simple online and realtime tracking with a deep association metric[J]. IEEE ICIP, 2017.
2. Zhang Y, Sun P, Jiang Y, et al. ByteTrack: Multi-object tracking by associating every detection box[C]. ECCV, 2022.
3. Aharon N, Orfaig R, Bobrovsky B-Z. BoT-SORT: Robust associations multi-pedestrian tracking[J]. 2022.
4. Kalman R E. A new approach to linear filtering and prediction problems[J]. Journal of Basic Engineering, 1960.
5. Bewley A, Ge Z, Ott L, Ramos F, Upcroft B. Simple online and realtime tracking[C]. ICIP, 2016.
6. Zhang Y, Wang C, Wang X, Zeng W, Liu W. FairMOT: On the fairness of detection and re-identification in multiple object tracking[C]. IJCV, 2021.
7. Li Y, Huang C, Chang C, et al. UAVDT: A benchmark for UAV object detection and tracking[C]. ECCV, 2018.
8. Dosovitskiy A, Ros G, Codevilla F, López A, Koltun V. CARLA: An open urban driving simulator[C]. CoRL, 2017.
9. Milan A, Leal-Taixé L, Reid I, Roth S, Schindler K. MOT16: A benchmark for multiple object tracking[J]. IJCV, 2016.
10. Qian H, et al. Low-altitude multi-object tracking via graph neural networks with cross-attention and reliable neighbor guidance[J]. Remote Sensing, 2025.
11. Wu P, et al. Multi-view, multi-target tracking in low-altitude scenes with UAV involvement[J]. Drones, 2025.
12. Ferreira D, Basiri M. Dynamic target tracking and following with UAVs using multi-target information[J]. Drones, 2024.
13. Zhang et al. Tracking by detection and query: An efficient end-to-end framework for multi-object tracking[J]. arXiv, 2024.
14. Lin T Y, Goyal P, Girshick R, et al. Focal loss for dense object detection[C]. ICCV, 2017.
15. Redmon J, Farhadi A. YOLOv3: An incremental improvement[J]. arXiv, 2018.
